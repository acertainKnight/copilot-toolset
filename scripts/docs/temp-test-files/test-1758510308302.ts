// Automatic tier placement based on content and access patterns
const memoryId = await threeTierManager.store(content, layer, tags, metadata);

// Intelligent search across all tiers
const results = await threeTierManager.search(query, options);

// Comprehensive statistics including tier performance
const stats = await threeTierManager.getStats();